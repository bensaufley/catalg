<!DOCTYPE html>
<html lang="en">
    <head>
        <title>{{.Title}}</title>
    </head>
    <body>
        <h1>{{.Title}}</h1>
        <p>{{.Username}},</p>
        <p>Your password reset token is:</p>
        <pre>
            <code>{{.Token}}</code>
        </pre>
    </body>
</html>
